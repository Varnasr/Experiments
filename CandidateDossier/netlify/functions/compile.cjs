// netlify/functions/compile.cjs
// CommonJS, unbundled. Robust compile with retry + diagnostics.
// All writes go to /tmp to avoid read-only FS. MUSL tectonic required.

const { spawn } = require("node:child_process");
const fs = require("node:fs").promises;
const { tmpdir } = require("node:os");
const { join } = require("node:path");

async function resolveTectonicPath() {
  const { access } = require("node:fs").promises;
  const candidates = [
    join(__dirname, "bin/tectonic"),
    join(__dirname, "../bin/tectonic"),
    process.env.LAMBDA_TASK_ROOT && join(process.env.LAMBDA_TASK_ROOT, "netlify/functions/bin/tectonic"),
    join(process.cwd(), "netlify/functions/bin/tectonic"),
  ].filter(Boolean);
  for (const p of candidates) { try { await access(p); return p; } catch {} }
  throw new Error("Tectonic binary not found. Tried:\n" + candidates.join("\n"));
}

// Safe template that always compiles
const SAFE_MAIN = String.raw`
\documentclass[12pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{lmodern}
\title{Candidate Dossier}
\author{Ummidvaar Dastaavez}
\date{\today}
\begin{document}
\maketitle
\section*{Candidate Blocks}
\input{candidate_blocks.tex}
\end{document}
`.trimStart();

// Minimal fallback (used on second attempt so the user still gets a PDF)
const FALLBACK_DOC = String.raw`
\documentclass[12pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{lmodern}
\begin{document}
\section*{Compilation Fallback}
This PDF was generated by the fallback pathway to ensure your users still receive output.
Please check the compile error details returned by the API for the exact LaTeX issue.
\end{document}
`.trimStart();

function sanitizeBlocks(input) {
  let s = (input || "").replace(/^\uFEFF/, "").replace(/\r/g, "");
  s = s.replace(/^\s*\\\\\s*/,'');                 // strip leading \\ (common cause)
  s = s.replace(/^\s*\\end\{[^\}]+\}\s*/, '');     // don't start with \end{...}
  if (!s.trim()) {
    s = String.raw`% placeholder
\noindent\textit{No candidate blocks were provided. Upload a CSV to generate them.}`;
  }
  return s;
}

function normalizeMain(mainTex) {
  const s = (mainTex || "").replace(/^\uFEFF/, "").replace(/\r/g, "");
  return /\\documentclass/.test(s) ? s : SAFE_MAIN;
}

async function runTectonic(tt, workDir, outDir, env) {
  return await new Promise((resolve, reject) => {
    const p = spawn(tt, ["-X", "compile", "main.tex", "--outdir", outDir], {
      stdio: ["ignore", "pipe", "pipe"],
      cwd: workDir,
      env
    });
    let stderr = "";
    p.stderr.on("data", d => { stderr += d.toString(); });
    p.on("error", reject);
    p.on("close", async (code) => {
      if (code !== 0) return reject(new Error(stderr || `tectonic exit ${code}`));
      try {
        const buf = await fs.readFile(join(outDir, "main.pdf"));
        resolve(buf);
      } catch (e) { reject(e); }
    });
  });
}

exports.handler = async (event) => {
  try {
    if (event.httpMethod !== "POST") {
      return { statusCode: 405, body: "Method not allowed" };
    }

    let body = {};
    try { body = JSON.parse(event.body || "{}"); } catch {}
    let { mainTex, blocksTex } = body;

    // Self-heal inputs
    mainTex   = normalizeMain(mainTex);
    blocksTex = sanitizeBlocks(blocksTex);

    // Work dirs under /tmp
    const base    = await fs.mkdtemp(join(tmpdir(), "tectonic-"));
    const workDir = join(base, "work");
    const outDir  = join(base, "out");
    const cache   = join(base, "cache");
    const homeDir = join(base, "home");
    const xdgCache= join(base, "xdg-cache");
    const xdgConf = join(base, "xdg-config");
    const texmfH  = join(base, "texmfhome");
    const texmfV  = join(base, "texmf-var");
    await Promise.all([
      fs.mkdir(workDir,{recursive:true}), fs.mkdir(outDir,{recursive:true}),
      fs.mkdir(cache,{recursive:true}),   fs.mkdir(homeDir,{recursive:true}),
      fs.mkdir(xdgCache,{recursive:true}),fs.mkdir(xdgConf,{recursive:true}),
      fs.mkdir(texmfH,{recursive:true}),  fs.mkdir(texmfV,{recursive:true})
    ]);

    // Write sources
    await fs.writeFile(join(workDir,"main.tex"),             mainTex,   "utf8");
    await fs.writeFile(join(workDir,"candidate_blocks.tex"), blocksTex, "utf8");

    const tt = await resolveTectonicPath();
    const env = {
      ...process.env,
      HOME: homeDir,
      TMPDIR: base,
      TECTONIC_CACHE_DIR: cache,
      XDG_CACHE_HOME: xdgCache,
      XDG_CONFIG_HOME: xdgConf,
      TEXMFHOME: texmfH,
      TEXMFVAR: texmfV,
    };

    // First attempt with provided/normalized TeX
    try {
      const pdf = await runTectonic(tt, workDir, outDir, env);
      return {
        statusCode: 200,
        headers: { "Content-Type": "application/pdf" },
        body: pdf.toString("base64"),
        isBase64Encoded: true
      };
    } catch (firstErr) {
      // Diagnostics: include beginnings of the files + stderr
      const preview = {
        error: String(firstErr.message || firstErr),
        main_head: (mainTex || "").slice(0, 200),
        blocks_head: (blocksTex || "").slice(0, 200)
      };

      // Second attempt: guaranteed fallback doc so user gets a PDF
      await fs.writeFile(join(workDir,"main.tex"), FALLBACK_DOC, "utf8");
      try {
        const pdf = await runTectonic(tt, workDir, outDir, env);
        // Return PDF, but annotate reason via header so UI can log it if needed
        return {
          statusCode: 200,
          headers: {
            "Content-Type": "application/pdf",
            "X-Compile-Diagnostics": Buffer.from(JSON.stringify(preview)).toString("base64")
          },
          body: pdf.toString("base64"),
          isBase64Encoded: true
        };
      } catch (secondErr) {
        // If even fallback fails, return diagnostics
        return {
          statusCode: 500,
          body: "Compile error (with fallback): " +
                JSON.stringify(preview, null, 2)
        };
      }
    }
  } catch (err) {
    return { statusCode: 500, body: "Compile error: " + err.message };
  }
};
